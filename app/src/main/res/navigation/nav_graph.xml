<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/tasks_fragment_dest">

    <fragment
        android:id="@+id/task_detail_fragment_dest"
        android:name="com.example.architectue_samplespractise.taskdetail.TaskDetailFragment"
        android:label="Task Details">
        <argument
            android:name="taskId"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/statistics_fragment_dest"
        android:name="com.example.architectue_samplespractise.statistics.StatisticsFragment"
        android:label="Todo">

    </fragment>

    <fragment
        android:id="@+id/tasks_fragment_dest"
        android:name="com.example.architectue_samplespractise.tasks.TasksFragment"
        android:label="Todo">
        <action
            android:id="@+id/action_tasksFragment_to_statisticsFragment"
            app:destination="@id/statistics_fragment_dest" />
        <action
            android:id="@+id/action_tasksFragment_to_taskDetailFragment"
            app:destination="@id/task_detail_fragment_dest" />
        <action
            android:id="@+id/action_tasksFragment_to_addEditTaskFragment"
            app:destination="@id/add_edit_task_fragment_dest" />
        <argument
            android:name="userMessage"
            android:defaultValue="0"
            app:argType="integer" />
    </fragment>

    <fragment
        android:id="@+id/add_edit_task_fragment_dest"
        android:name="com.example.architectue_samplespractise.addedittask.AddEditTaskFragment"
        android:label="{title}">
        <argument
            android:name="taskId"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="title"
            app:argType="string"
            app:nullable="false" />

    </fragment>

</navigation>